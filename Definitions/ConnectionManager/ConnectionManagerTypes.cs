namespace z.ETL.ConnectionManager
{
    public enum ConnectionManagerType
    {
        Unknown,
        SqlServer,
        Adomd,
        SQLite,
        MySql,
        Postgres,
        Access
    }
}
